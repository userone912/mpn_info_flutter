import 'package:flutter/material.dart';

class DatabaseSettingsPage extends StatelessWidget {
  const DatabaseSettingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pengaturan Database')),
      body: Center(
        child: Text('Fitur pengaturan database akan segera hadir.'),
      ),
    );
  }
}
